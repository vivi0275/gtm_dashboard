{
  "name": "tandem-outbound-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Live outbound dashboard: static page + serverless lemlist proxy (key stays server-side).",
  "engines": { "node": ">=18" }
}
